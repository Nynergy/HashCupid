#ifndef HASH_BB
#define HASH_BB

/* TODO: Create a hash algorithm to be used for computeHash() */
int computeHash(char * name) {
	int hash;
	return hash;
}

/* TODO: Create a method of comparing the hashes to make a match */
int computeMatch(int hash_one, int hash_two) {
	int match;
	return match;
}

#endif
